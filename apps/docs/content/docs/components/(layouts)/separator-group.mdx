---
title: SeparatorGroup
description: Groups multiple separators with optional captions in a consistent layout
---

<ComponentPreview name="separator-group:basic" />

## Usage
The `SeparatorGroup` component provides a consistent layout for separating sections with horizontal or vertical rules. It consists of a flex container with automatically spaced children.

- `SeparatorGroup.Root`: The flex container that holds the separators.
- `SeparatorGroup.Item`: The individual separator element.
- `SeparatorGroup.Caption`: An optional caption displayed alongside the separator group.

```tsx
import { SeparatorGroup } from "@moto-ui/react"

<SeparatorGroup.Root>
  <SeparatorGroup.Item />
</SeparatorGroup.Root>
```


## Caption
Use `SeparatorGroup.Caption` to add a label next to a separator for context.

<ComponentPreview name="separator-group:caption" />

## Props

**Root**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| orientation | `"horizontal" \| "vertical"` | `"horizontal"` | The orientation of the separator group. |
| as | `React.ElementType` | `div` | The underlying element to render. |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**Item**

The `SeparatorGroup.Item` component accepts all props from the `Separator` component.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| orientation | `"horizontal" \| "vertical"` | `"horizontal"` | The orientation of the separator. |
| size | `"xs" \| "sm" \| "md" \| "lg" \| "xl"` | `"sm"` | The thickness of the separator. |
| as | `React.ElementType` | `div` | The underlying element to render. |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**Caption**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| as | `React.ElementType` | `span` | The underlying element to render. |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

## Data Attributes

**Root**

| Attribute | Description |
| --- | --- |
| data-scope | `separator-group` |
| data-part | `root` |
| data-orientation | The orientation of the separator group. |

**Item**

| Attribute | Description |
| --- | --- |
| data-scope | `separator` |
| data-part | `root` |
| data-orientation | The orientation of the separator. |

**Caption**

| Attribute | Description |
| --- | --- |
| data-scope | `separator-group` |
| data-part | `caption` |

## Accessibility
- `SeparatorGroup.Root` renders a `div` with `role="separator"`.
- `SeparatorGroup.Caption` is rendered as a `span` and should be used to provide context when the separator's purpose is not purely visual.
- Separators are inherently hidden from assistive technology when used in a purely decorative role.
