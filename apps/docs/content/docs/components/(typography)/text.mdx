---
title: Text
description: Renders text and paragraphs in the interface.
---

<Example name="text:basic" />

## Usage
The `Text` component simplifies styling with design tokens and an ergonomic API for responsive designs.
```tsx
import { Text } from "@elemnts-ui/react"
```

```tsx
<Text>
  {...content}
</Text>
```

## Sizes
Control text size with the `fontSize` or `textStyle` prop.

<Example name="text:sizes" />

## Weights
Control font weight using the `fontWeight` prop.

<Example name="text:weights" />

## Truncation
Use the `truncate` prop to limit the text to a single line.

<Example name="text:truncate" />

## Line Clamp
Use the `lineClamp` prop to limit content to a specified number of lines, truncating any overflow.
<Example name="text:line-clamp" />
