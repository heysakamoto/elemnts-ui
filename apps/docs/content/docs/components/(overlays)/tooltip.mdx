---
title: Tooltip
description: Displays supplementary information when a user hovers over an element.
---

<ComponentPreview name="tooltip:basic" />

### Usage
The `Tooltip` component is used to reveal supplemental information when a user hovers or focuses on a target element.

- `Tooltip.Root`: The root container for the tooltip.
- `Tooltip.RootProvider`: Provides the tooltip context to its children.
- `Tooltip.Trigger`: The target element that triggers the tooltip.
- `Tooltip.Positioner`: Manages the absolute positioning of the tooltip.
- `Tooltip.Content`: The visual container for the tooltip's content.
- `Tooltip.Arrow`: An optional arrow that points to the trigger.
- `Tooltip.ArrowTip`: The tip of the arrow.
- `Tooltip.Context`: Access the tooltip state and methods.

```tsx
import { Tooltip, Portal, Surface, Button } from "@moto-ui/react"

<Tooltip.Root>
  <Tooltip.Trigger asChild>
    <Button>Hover me</Button>
  </Tooltip.Trigger>
  <Portal>
    <Tooltip.Positioner>
      <Tooltip.Content asChild>
        <Surface>
          <Surface.Content>Tooltip content</Surface.Content>
        </Surface>
      </Tooltip.Content>
    </Tooltip.Positioner>
  </Portal>
</Tooltip.Root>
```

### Delay
Set the show and hide delays for the tooltip using the `openDelay` and `closeDelay` props.

<ComponentPreview name="tooltip:delay" />

### Interactive
Enable the `interactive` prop to keep the tooltip open while users interact within its content (for example, links or buttons).

<ComponentPreview name="tooltip:interactive" />

### Props

**Root**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| aria-label | `string` | - | Custom label for the tooltip. |
| closeDelay | `number` | `150` | The close delay of the tooltip. |
| closeOnClick | `boolean` | `true` | Whether the tooltip should close on click. |
| closeOnEscape | `boolean` | `true` | Whether to close the tooltip when the Escape key is pressed. |
| closeOnPointerDown | `boolean` | `true` | Whether to close the tooltip on pointerdown. |
| closeOnScroll | `boolean` | `true` | Whether the tooltip should close on scroll. |
| defaultOpen | `boolean` | - | The initial open state of the tooltip. |
| disabled | `boolean` | - | Whether the tooltip is disabled. |
| id | `string` | - | The unique identifier of the machine. |
| ids | `Partial<{ trigger: string, content: string, arrow: string, positioner: string }>` | - | The ids of the elements in the tooltip. |
| immediate | `boolean` | - | Whether to synchronize the present change immediately. |
| interactive | `boolean` | `false` | Whether the tooltip's content is interactive. |
| lazyMount | `boolean` | `false` | Whether to enable lazy mounting. |
| open | `boolean` | - | The controlled open state of the tooltip. |
| openDelay | `number` | `400` | The open delay of the tooltip. |
| positioning | `PositioningOptions` | - | The options used to position the tooltip. |
| present | `boolean` | - | Whether the node is present (controlled by the user). |
| skipAnimationOnMount | `boolean` | `false` | Whether to allow the initial presence animation. |
| unmountOnExit | `boolean` | `false` | Whether to unmount on exit. |
| onExitComplete | `VoidFunction` | - | Function called when the animation ends. |
| onOpenChange | `(details: OpenChangeDetails) => void` | - | Function called when the tooltip is opened or closed. |

**RootProvider**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| value | `UseTooltipReturn` | - | **Required**. The value returned from `useTooltip`. |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**Trigger**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**Positioner**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**Content**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**Arrow**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |

**ArrowTip**

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| asChild | `boolean` | `false` | Use the provided child element as the default rendered element. |


### Data Attributes

**Trigger**

| Attribute | Description |
| --- | --- |
| data-scope | `tooltip` |
| data-part | `trigger` |
| data-state | The state of the tooltip. `open` or `closed`. |
| data-expanded | Present when expanded. |

**Content**

| Attribute | Description |
| --- | --- |
| data-scope | `tooltip` |
| data-part | `content` |
| data-state | The state of the tooltip. `open` or `closed`. |
| data-placement | The placement of the content. |

### CSS Variables

**Positioner**

| Variable | Description |
| --- | --- |
| --x | The x position for transform. |
| --y | The y position for transform. |
| --z-index | The z-index value. |
| --transform-origin | The transform origin for animations. |
| --available-width | The available width in viewport. |
| --available-height | The available height in viewport. |

**Arrow**

| Variable | Description |
| --- | --- |
| --arrow-size | The size of the arrow. |
| --arrow-background | The background color of the arrow. |
