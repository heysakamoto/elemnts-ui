import { LandingFeatureHero } from "./hero";
import { LandingFeatureInstall } from "./install";

export const LandingFeature = Object.assign(
	{},
	{
		Hero: LandingFeatureHero,
		Install: LandingFeatureInstall,
	},
);
