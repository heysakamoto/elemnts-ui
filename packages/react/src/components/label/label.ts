import { ark } from "@ark-ui/react";
import { styled } from "@moto-ui/styled-system/jsx";
import { labelRecipe } from "@moto-ui/styled-system/recipes";

export const Label = styled(ark.div, labelRecipe);
Label.displayName = "Label";
