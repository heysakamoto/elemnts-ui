import { ark } from "@ark-ui/react";
import { styled } from "../../styled-system/jsx";
import { labelRecipe } from "../../styled-system/recipes";

export const Label = styled(ark.div, labelRecipe);
Label.displayName = "Label";
