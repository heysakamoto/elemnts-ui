import { ark } from "@ark-ui/react";
import { styled } from "../../styled-system/jsx";
import { markRecipe } from "../../styled-system/recipes";

export const Mark = styled(ark.mark, markRecipe);
Mark.displayName = "Mark";
