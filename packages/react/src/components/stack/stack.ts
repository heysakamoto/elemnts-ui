import { ark } from "@ark-ui/react";

import { styled } from "../../../styled-system/jsx";
import { stackRecipe } from "../../../styled-system/recipes";

export const Stack = styled(ark.div, stackRecipe);
Stack.displayName = "Stack";
