import { ark } from "@ark-ui/react";
import { styled } from "@moto-ui/styled-system/jsx";
import { stackRecipe } from "@moto-ui/styled-system/recipes";

export const Stack = styled(ark.div, stackRecipe);
Stack.displayName = "Stack";
