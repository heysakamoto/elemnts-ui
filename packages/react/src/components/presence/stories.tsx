import Basic from "./examples/basic";
import LazyMount from "./examples/lazy-mount";
import Unmount from "./examples/unmount";

const meta = {
	title: "Presence",
	tags: ["autodocs"],
};

export default meta;

export { Basic, LazyMount, Unmount };
