import { ark } from "@ark-ui/react";
import { styled } from "@moto-ui/styled-system/jsx";
import { textRecipe } from "@moto-ui/styled-system/recipes";

export const Text = styled(ark.p, textRecipe);
Text.displayName = "Text";
