import Basic from "./examples/basic";
import Fallback from "./examples/fallback";
import RenderFn from "./examples/render-fn";

const meta = {
	title: "Client Only",
	tags: ["autodocs"],
};

export default meta;

export { Basic, Fallback, RenderFn };
