import Basic from "./examples/basic";
import Fallback from "./examples/fallback";
import Nested from "./examples/nested";
import RenderFn from "./examples/render-fn";

const meta = {
	title: "Show",
	tags: ["autodocs"],
};

export default meta;

export { Basic, Fallback, Nested, RenderFn };
