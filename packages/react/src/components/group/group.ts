import { ark } from "@ark-ui/react";
import { styled } from "../../styled-system/jsx";
import { groupRecipe } from "../../styled-system/recipes";

export const Group = styled(ark.div, groupRecipe);
Group.displayName = "Group";
