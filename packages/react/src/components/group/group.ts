import { ark } from "@ark-ui/react";
import { styled } from "@moto-ui/styled-system/jsx";
import { groupRecipe } from "@moto-ui/styled-system/recipes";

export const Group = styled(ark.div, groupRecipe);
Group.displayName = "Group";
