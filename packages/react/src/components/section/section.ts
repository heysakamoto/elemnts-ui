import { ark } from "@ark-ui/react";

import { styled } from "../../../styled-system/jsx";
import { sectionRecipe } from "../../../styled-system/recipes";

export const Section = styled(ark.section, sectionRecipe);
Section.displayName = "Section";
