import { ark } from "@ark-ui/react";
import { styled } from "@moto-ui/styled-system/jsx";
import { inputRecipe } from "@moto-ui/styled-system/recipes";

export const Input = styled(ark.input, inputRecipe);
Input.displayName = "Input";
